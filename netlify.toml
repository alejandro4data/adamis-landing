# 1. El Guardián (Protege la carpeta /app/)
[[edge_functions]]
  path = "/app/*"
  function = "guardian"

# 2. El Login (Procesa el formulario)
[[edge_functions]]
  path = "/auth-login"   # Esta es la URL "falsa" que procesará el login
  function = "login"

# 3. CABECERAS DE SEGURIDAD (Aquí es donde estaba el bloqueo)
[[headers]]
  for = "/*"
  [headers.values]
    Content-Security-Policy = "default-src 'self'; script-src 'self' 'unsafe-inline' https://unpkg.com https://cdn.tailwindcss.com https://cdnjs.cloudflare.com; style-src 'self' 'unsafe-inline' https://fonts.googleapis.com https://unpkg.com; font-src 'self' https://fonts.gstatic.com https://unpkg.com data:; img-src 'self' data: https:;"
    
    X-Frame-Options = "DENY"
    X-XSS-Protection = "1; mode=block"